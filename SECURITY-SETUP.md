# SECURITY SETUP - IMPORTANT! 🔒

## خطوات تأمين لوحة الأدمن

### 1. تأكد من وجود NEXTAUTH_SECRET في .env.local

افتح ملف `.env.local` وتأكد من وجود السطر التالي:

```env
NEXTAUTH_SECRET=your-very-long-random-secret-key-here
```

**لإنشاء NEXTAUTH_SECRET آمن:**

في PowerShell، قم بتشغيل:

```powershell
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

أو استخدم هذا الرابط: https://generate-secret.vercel.app/32

انسخ النتيجة وضعها في `.env.local` بدلاً من `your-very-long-random-secret-key-here`

### 2. أعد تشغيل السيرفر

بعد تعديل `.env.local`:

```powershell
# أوقف السيرفر (Ctrl+C)
# ثم شغله من جديد
npm run dev
```

### 3. تأكد من تسجيل الخروج

إذا كنت مسجل دخول حالياً:

1. اذهب إلى أي صفحة في الموقع
2. اضغط Logout من Navbar
3. حاول الدخول إلى `/admin/dashboard` مباشرة
4. يجب أن يتم توجيهك إلى `/admin/login` تلقائياً

### 4. اختبار الحماية

جرب هذه الروابط بدون تسجيل دخول:

- http://localhost:3000/admin/dashboard
- http://localhost:3000/admin/services
- http://localhost:3000/admin/orders
- http://localhost:3000/admin/portfolio

**النتيجة المتوقعة:** يجب أن تُوجَّه إلى `/admin/login` في كل مرة

---

## التحديثات الأمنية المضافة:

### ✅ ProtectedRoute محسّن

- فحص دقيق للـ session
- منع عرض المحتوى قبل التحقق
- إعادة توجيه فورية للمستخدمين غير المصرح لهم

### ✅ Middleware Protection

- حماية على مستوى السيرفر
- فحص الـ token قبل الوصول للصفحة
- تطبق على جميع مسارات `/admin/*`

### ✅ Session Management

- JWT tokens مشفرة
- صلاحية 30 يوم
- تسجيل خروج تلقائي بعد انتهاء الصلاحية

---

## ⚠️ ملاحظات مهمة:

1. **لا تشارك NEXTAUTH_SECRET أبداً**

   - لا تضعه في GitHub
   - لا ترسله لأي شخص
   - غيّره كل فترة

2. **في الإنتاج (Production):**

   ```env
   NEXTAUTH_URL=https://yourdomain.com
   NEXTAUTH_SECRET=<your-production-secret>
   ```

3. **للتأكد من الحماية:**
   - افتح نافذة Incognito/Private
   - حاول الوصول لـ `/admin/dashboard`
   - يجب أن يطلب تسجيل دخول

---

## إذا واجهت مشاكل:

### المشكلة: لا يزال يمكن الدخول بدون تسجيل دخول

**الحل:**

```powershell
# احذف .next folder
Remove-Item -Recurse -Force .next
# أعد تشغيل
npm run dev
```

### المشكلة: Session expired باستمرار

**الحل:**

- تأكد من `NEXTAUTH_SECRET` موجود
- تأكد من `NEXTAUTH_URL` صحيح
- امسح cookies المتصفح

---

تم إنشاء هذا الملف في: November 28, 2025
